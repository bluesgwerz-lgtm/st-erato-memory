# Erato Memory — 设计稿（v0.1 2026-09-02 → v0.3 2026-09-03，后期定案见 §14–§15）

SillyTavern 第三方扩展，为 Erato 预设提供结构化长期记忆，与预设的恒定窗口正则组严格对齐。
手机端酒馆使用，GitHub 安装，本机无酒馆，全部由真机实测。

---

## 0. 已定案的决策（讨论结论，不再重开）

| 项 | 决策 |
|---|---|
| 存储 | 本地 `chat_metadata.eratoMemory`，随聊天文件保存；不上云 |
| 双端 | 扩展跑在打开酒馆页面的浏览器里，手机/电脑都可用；两端连同一个酒馆实例时记忆随聊天文件自动共享，各装各的酒馆则各自独立 |
| 摘要模型 | 独立副 API（OpenAI 兼容，地址/密钥/模型可配），经酒馆后端转发，不碰主模型连接 |
| 入库时机 | ~~每楼一次~~ → **v0.3 窗口制**：攒够 `autoInterval` 楼自动跑一次，一次调用最多 `windowFloors` 楼，按事件切条目；手动「总结到当前」不看间隔（见 §15）。**v0.3.2 起楼数按酒馆楼层计**，默认 20 / 40（见 §16） |
| 注入范围 | 只注入 depth≥6（正文已被 6🌸 剥离）的楼层对应条目；正文与摘要不同时在窗口。**v0.2 起 depth 按可见楼层数计算**（隐藏楼层不占深度，与酒馆正则口径一致） |
| 注入方式 | `setExtensionPrompt` in-chat，depth 6，system 角色 |
| 等级 | S/A/B/C 由副 AI 判，用户可手动钉 S；衰减按楼层数不按墙上时间 |
| 隔离 | 记忆按聊天隔离；分支自动继承（元数据随复制）+ 切聊天时对账作废越界条目 |
| 面板 | 必做，手机优先；可看/改等级/改文字/删/重摘要/钉 S。**0902 实测后重排，见 §14** |
| 入口 | ~~顶栏图标~~ → **悬浮球 + 魔杖菜单**（顶栏图标已废，手机端挤压其他图标，见 §14.1） |
| 隐藏楼层 | **总结完成后自动隐藏已总结楼层，保留最近 N 楼（默认 6）可见，可逆**（见 §14.3；推翻 v0.1「插件不动楼层可见性」的判断） |
| 失败 | 副 API 拒答或超时 → 标记待重试 + 面板显眼提示，下轮自动重试，绝不静默跳过 |
| 向量召回 | ~~二期。200 楼以内全量顺序注入足够~~ → **v0.4 草案见 §17**：走酒馆自带 `/api/vector/*`，只捞被预算裁掉的条目，默认关 |
| recap | 主模型每楼产出的 `<recap>` 是摘要输入之一，日期/时段/场景直接沿用 |

---

## 1. 与 Erato 预设的接口事实（Erato0814.json 实测）

depth 从最后一条消息倒数，0 = 最新；用户消息与 AI 消息各占一层。以下正则全部 promptOnly，
聊天文件里存的是全量原文。

| depth | 被剥掉 | 正则 |
|---|---|---|
| ≥2 | 历史用户输入（只留最新一条） | 13🌸 |
| ≥4 | 状态栏 `<details><summary>📍…` | 4🌸 |
| ≥6 | `<content>…</content>` 正文 | 6🌸 |
| ≥20 | `<recap>…</recap>` | 5🌸 |
| ≥10 全剥 | 14🌸 **目前关闭** | — |

每楼 AI 消息的原文结构（顺序）：
`说戏 COT（<think_format>/<COT_Director>，可达数千 token）` → `<content>正文（含 :::newspaper 卷引）</content>`
→ `<details><summary>📍状态栏</summary>…</details>` → `<recap><details><summary>📜 摘要</summary>**日期 · 时段 · 场景**\n叙述</details></recap>`
→ `<plot_directions>…</plot_directions>`；另有 `<!--prop-->…<!--/prop-->` 道具卡与 `‹toy:xxx›` 玩具标记随机出现。

扩展提示不是消息，不受任何正则影响；注入块用 `<erato_memory>` 标签包裹，与预设现有标签零冲突。

**预设侧唯一可选改动**：插件跑稳后把 5🌸 的 minDepth 从 20 改成 6，省掉 depth 6–19 的 recap。
MVP 阶段不改，两者并存无害。

---

## 2. 架构

```
[采集] 事件钩子 → 抠取楼层文本(content/recap/前一条用户消息) → 内容哈希
   ↓
[摘要] 副 API(经酒馆后端转发) → JSON 条目(带等级) → JSON 修复 → 写入 chat_metadata
   ↓
[对账] CHAT_CHANGED / 删楼 / 编辑 → 孤立/过期条目标记 → 过期自动重摘要
   ↓
[注入] generate_interceptor → 过滤(depth≥6, status ok) → 排序 → 预算裁剪 → setExtensionPrompt(depth 6)
   ↓
[治理] 衰减权重 → B/C 归档合并(非破坏) → S 正典压缩(有上限)
   ↓
[面板] 手机全屏浮层：列表/筛选/编辑/钉S/重摘要/补齐/对账/导出导入
```

代码组织：单入口 `index.js`（IIFE，`SillyTavern.getContext()`，与 st-toy-sync 同风格），
按职责拆成几个内部对象（`collector / summarizer / store / injector / governor / panel`），不引入构建工具。

`manifest.json` 关键字段：`"generate_interceptor": "eratoMemory_intercept"`，`"loading_order": 100`。

---

## 3. 数据结构

存放位置：`ctx.chatMetadata.eratoMemory`，通过 `ctx.saveMetadata()` / `saveMetadataDebounced` 持久化。

```js
{
  version: 1,
  entries: [ Entry ],          // 按 src.idx 升序维护
  archives: [ Archive ],       // B/C 归档段
  canon: { text: '', builtFrom: [ids], builtAt: 0 },   // S 正典压缩文本（S 条目超上限时才生成）
  skip: { [send_date]: true }, // 用户手动标记"不入库"的楼层
  stats: { lastIngestAt, lastError, failStreak }
}

Entry = {
  id: 'em_k3x9a1',
  src: {
    idx: 12,                 // AI 楼层在 chat[] 中的下标（对账时可修正）
    send_date: '…',          // 酒馆消息自带，跨删楼定位用
    hash: 'a91f…',           // 仅对抠出的 <content> 文本做哈希，改状态栏不算改
    user_idx: 11, user_hash: '…'   // 前一条用户消息（可为空）
  },
  story_time: '2025年3月4日 · 午后两点 · 陆衍家·厨房',   // 直接取 recap 锚点行
  type: 'plot' | 'emotion' | 'intimacy' | 'relationship' | 'setting',
  title: '≤8字',
  summary: '≤120字，因果链，写"为什么"不只写"做了什么"',
  characters: ['陆衍', '{{user}}'],
  emotion_shift: '陆衍：戒备→动摇',            // 可空
  known_by: ['陆衍'],                          // 信息差：谁知道这件事；空=在场者皆知
  tags: [],
  intimacy: null | { acts: '', consent: '', firsts: '', aftermath: '' },   // 仅 type=intimacy
  related_to: ['em_…'],                        // 因果前驱
  grade: 'S' | 'A' | 'B' | 'C',
  pinned: false,                               // 用户钉 S；pinned 时 grade 锁死为 S
  status: 'ok' | 'pending' | 'failed' | 'stale' | 'orphan' | 'archived',
  attempts: 0, last_error: '',
  model: 'deepseek-chat', created_at: 0, updated_at: 0
}

Archive = {
  id: 'ar_…', covers: ['em_…', …], story_time_range: '3月4日–3月9日',
  text: '一段 150–250 字的合并叙述', created_at: 0
}
```

体积估算：每条约 600 字节，300 楼 ≈ 150 条 ≈ 90 KB，chat_metadata 完全承受。
**向量不进 chat_metadata**：几百条浮点数组会让聊天文件每次防抖保存都重新序列化整份，二期另存。

---

## 4. 事件钩子与流程

| 事件 | 动作 |
|---|---|
| `CHAT_CHANGED` | 取消进行中的摘要任务 → 对账 → 重建注入 → 触发补齐（延迟 1.5s，避免与酒馆加载抢） |
| `MESSAGE_RECEIVED` / `CHARACTER_MESSAGE_RENDERED` | 防抖 800ms → 触发补齐 |
| `MESSAGE_DELETED` | 对账（标 orphan、修正 idx）→ 重建注入 |
| `MESSAGE_UPDATED` | 对账（哈希变化标 stale → 重摘要） |
| `MESSAGE_SWIPED` | 不处理。depth 0 的楼层永远不入库，swipe 只影响 depth 0 |
| `generate_interceptor` | 见 §7；`type === 'quiet'` 时直接返回 |

### 4.1 补齐（ingest）

```
for i in 0..chat.length-1:
  m = chat[i]
  if m.is_user or m.is_system: continue
  depth = chat.length-1-i
  if depth < 2: continue                       // 还可能被 swipe/重roll
  if skip[m.send_date]: continue
  e = entries.find(src.send_date == m.send_date)
  if e and e.status in (ok, archived, pending-with-attempts<3): continue
  if e and e.status == stale: 重摘要并原地替换
  else: 新建 pending 条目 → 摘要 → ok / failed
```

单飞锁：内存变量 + 当前 chatId，切聊天即失效。**不把锁写进 metadata**，否则刷新页面或异常中断后锁会残留在聊天文件里。
串行执行，每条之间不并发，避免副 API 限流；一次补齐最多处理 N 条（默认 20）后让出，下次事件继续。

### 4.2 对账（reconcile）

对每条 entry：
1. `chat[src.idx]?.send_date === src.send_date` → 命中；否则按 send_date 全表搜，找到则修正 idx。
2. 找不到 → `orphan`（面板显示，可删可留，不注入）。
3. 找到但 `hash(content) !== src.hash` → `stale`（下次补齐自动重摘要）。
4. `src.idx > chat.length-1` 的一律 orphan —— 这就是分支/检查点场景：新文件只复制到分支点，之后的条目全部越界作废。

---

## 5. 楼层文本抠取

输入给摘要模型的文本 = 三段拼接，每段独立抠取：

**A. 前一条用户消息**（`chat[idx-1]`，若是用户消息）：原文，仅剥 `<!--prop-->` 与玩具标记。
提示词里声明"括号内 OOC 指令不是剧情"。

**B. 正文**：
1. 优先 `<content>([\s\S]*?)<\/content>` 第一个匹配。
2. 缺标签兜底：整条消息依次剥 `<think_format>…</think_format>`、`<COT_Director>…</COT_Director>`、
   `<thinking>…</thinking>`、所有 `<details>…</details>`、`<recap>…</recap>`、`<plot_directions>…</plot_directions>`、
   `<!--prop-->…<!--/prop-->`、`‹toy:x›`，剩余部分当正文，并在条目上记 `src.fallback = true`。
3. 两种情况都再剥 `:::newspaper … :::` 卷引块。
4. 哈希只对 B 的结果做。

**C. recap**：`<recap>` 内取 `**…**` 加粗行为 `story_time`，其后文本为 recap 叙述。缺失则 story_time 留空，
提示词要求模型从正文推断，推断不出写"（未知）"，禁止编造。

说戏 COT 绝不进入摘要输入——它含备选方案与未采用的走向，混入会造成假记忆。

---

## 6. 摘要引擎

### 6.1 API 路由（酒馆后端的公开行为：`reverse_proxy` / `proxy_password` 覆盖请求目标与密钥）

```js
fetch('/api/backends/chat-completions/generate', {
  method: 'POST',
  headers: ctx.getRequestHeaders(),
  body: JSON.stringify({
    chat_completion_source: 'openai',
    reverse_proxy: baseUrl,          // 用户填的地址，去掉尾部 /chat/completions
    proxy_password: apiKey,
    model, messages,
    temperature: 0.3, max_tokens: 900,
    stream: false,
  }),
})
```
不受浏览器跨域限制，不碰主模型密钥，不切 connection profile。Gemini 走其 OpenAI 兼容端点即可。
拉模型列表走同一后端的 `/api/backends/chat-completions/status`（主 API「连接」按钮同路），后端向 `{url}/models` 发 GET；
上游出错时酒馆只回 `{error:true}` 不带原因，前端提示去看后台日志并允许手填模型名。
超时默认 60s（AbortController）。可选**备用模型**（第二组 url/key/model）：主副 API 连续失败或返回空时自动换。

### 6.2 提示词初稿

system：
```
你是成人向互动小说的档案员。你的工作是把一段已发生的剧情整理成一条结构化记忆，供后续写作参考。
只输出 JSON，不输出任何其他文字。用中性、克制、事实性的语言，不渲染不评价，性内容按事实记录不省略。
```

user：
```
## 已知主角
用户角色：{{name1}}；对手角色：{{name2}}。其余出场者按原文名字记录。

## 最近三条记忆（供承接因果与去重，不要重复其中已记录的事实）
{{last3: [id] story_time | title | summary}}

## 本楼材料
### 用户的行动（括号内的 OOC 指令不是剧情，忽略）
{{userText 或 "（无）"}}

### 正文
{{content}}

### 作者摘要（日期·时段·场景 与 叙述，若有）
{{recap 或 "（无）"}}

## 输出要求
输出一个 JSON 对象：
{
  "story_time": "沿用作者摘要的『日期 · 时段 · 场景』原文；没有则从正文推断；推断不出写（未知），禁止编造",
  "type": "plot | emotion | intimacy | relationship | setting 之一。setting=新角色登场/世界观揭示/规则确立",
  "title": "≤8字，意象或事件名，不用『之后』『开始』这类空词",
  "summary": "≤120字。以事件为单位写起因→经过→结果，写『为什么』而不只是『做了什么』。可保留1句决定走向的原台词。去掉感官修辞。",
  "characters": ["在场且有行动的人"],
  "emotion_shift": "『角色：A→B』格式，一人一句，无变化留空",
  "known_by": ["知道这件事的角色。若是秘密/信息差，只列知情者；全员在场则留空数组"],
  "tags": ["3-6个检索词：人名/地点/物件/情绪/主题"],
  "intimacy": null 或 {"acts": "行为要点，事实性", "consent": "同意状态与主动方", "firsts": "第一次的事项，没有留空", "aftermath": "事后状态与余波"},
  "related_to": ["最近三条记忆里与本楼有因果关系的 id，没有留空数组"],
  "grade": "S | A | B | C",
  "grade_reason": "≤20字"
}

等级标准（按语义重要度，不按篇幅）：
S = 不可逆事实：死亡/告白成立/关系定名/身份揭露/立誓承诺/任何『第一次』。永不遗忘。
A = 关系转折、重大冲突、重要秘密或信息差的建立、角色重大决定。
B = 推动剧情但可被概括的普通事件。
C = 日常、闲聊、氛围、无后果的互动。
拿不准时降一级，不要升级。

{{用户自定义指令，可空}}
```

`{{name1}}/{{name2}}` 取自 `ctx.name1/name2`。用户自定义指令来自设置项「记忆导演指令」，
用于"重点记录 X"之类的偏好。

### 6.3 解析与失败

- 解析链：```json 代码块 → 最外层 `{…}` → 弯引号修复 → 字符串内裸换行/未转义引号修复 → 截断补闭合。
  覆盖模型实际会犯的三类错：字符串里的裸引号、字符串里的真换行、max_tokens 截断。
- 校验：`summary` ≥10 字、`grade` 合法、`type` 合法，否则视为失败。
- 失败：`status=failed, attempts++`，记录 `last_error`（HTTP 码 / 空回复 / 解析失败 / 疑似拒答）。
  `attempts<3` 时下次补齐自动重试；`≥3` 停止自动重试，面板顶部红条提示「N 条待处理」，可手动重试或切备用模型。
- **疑似拒答判定**：回复含 "无法" / "不能协助" / "违反" / "I can't" / "I cannot" 且无 JSON → 直接标 `refused`，
  有备用模型则立即换模型重试一次。这是 🚗 组楼层的主要失败路径，必须可见。

---

## 7. 注入层

### 7.1 触发

`window.eratoMemory_intercept = async (chat, contextSize, abort, type)`：
`type === 'quiet'` 返回；否则 `reconcile()` → `buildBlock()` → `setExtensionPrompt('erato_memory', text, IN_CHAT, depth, false, SYSTEM)`。
MVP 无网络调用，零延迟。`CHAT_CHANGED` 时同样重建，空聊天则清空。

### 7.2 过滤与排序

- 取 `status === 'ok'` 且 `src.idx <= chat.length - 1 - contentWindow`（`contentWindow` 默认 6，与 6🌸 一致，可配）。
- 归档段替代其 `covers` 内的条目。
- 按 `src.idx` 升序（时间顺序），S/pinned 另抽一份进正典区。

### 7.3 预算

`maxInjectChars` 默认 9000 字（≈6k token）。超出时按顺序丢弃：C 最老 → B 最老 → 触发治理归档（§8）。
S/A 不丢，S 超上限走正典压缩。丢弃时面板显示「本轮 N 条未注入」。

### 7.4 格式

```
<erato_memory>
[长期记忆 · 由记忆插件维护 · 覆盖最近三回合之前的全部剧情，按时间顺序]
[仅作为已发生事实使用：不复述、不总结、不预告；信息差按「知情」栏执行，未列名者不知情]

## 正典
- {story_time}「{title}」{summary}
…

## 往事
- {story_time}「{title}」({grade}) {summary}｜情绪：{emotion_shift}｜知情：{known_by}
- （归档）{story_time_range}：{archive.text}
…
</erato_memory>
```
intimacy 字段以「亲密：acts；consent；firsts；aftermath」一行附在该条之后，仅在 `type=intimacy` 时输出。
`known_by` 为空时不输出「知情」栏。

### 7.5 depth 为什么是 6

depth 6 正好落在全文窗口的外沿：注入块 → depth 6–19 的 recap → 最近三回合全文，时间顺序自然。
预设自身的 in-chat 模块集中在 depth 4/2/0，不冲突。可配（0–20）。

---

## 8. 记忆治理

### 8.1 衰减

`age = chat.length - 1 - src.idx`（楼层数）。`weight = base[grade] × 0.5^(age / halfLife[grade])`。
`base = S:∞, A:1.0, B:0.6, C:0.3`；`halfLife = S:∞, A:200, B:60, C:20`。
MVP 只在预算裁剪时用作排序依据；二期向量召回时与相似度加权。

### 8.2 归档（非破坏）

条件：`age > 40` 的 B/C 条目 ≥ 12 条。取最老的一段（按 story_time 相邻，8–12 条），副 API 合并成一段
150–250 字叙述 → 新建 Archive，原条目 `status=archived`（保留原文，面板可看可恢复，恢复即删除对应 Archive）。
在补齐流程末尾执行，与摘要同一队列，不阻塞生成。

合并提示词要点：按时间顺序、保留人名地名与因果、保留每条的 known_by 信息差、去重、不加新事实。

### 8.3 正典上限

S + pinned 条目 ≤ 25 时逐条注入；超过时副 API 把全部 S 压成 ≤600 字正典段 `canon.text`，
之后 S 集合每变化 5 条重压一次。S 条目本身永不归档、永不删除（除非用户手动删）。

---

## 9. 面板（手机优先）

入口三处：顶栏图标（与 toy-sync 状态灯同位置风格，不挂酒馆 `.drawer-toggle` 类，免得被酒馆抽屉处理器抢事件）
+ 输入框左侧魔杖菜单 `#extensionsMenu` 里的「记忆面板」项 + 扩展设置抽屉里的按钮。
形态：`position:fixed; inset:0` 全屏浮层，顶部工具条带「记忆 / 设置」两页切换，主体纵向滚动，底部操作条。不用 `prompt()/confirm()`。
未配副 API 且无条目时，打开面板直接落到设置页。

「记忆」页顶部：状态摘要「已入库 N / 待处理 N / 失败 N / 本轮注入 N 条 ≈ M 字」；筛选：等级 / 类型 / 状态；搜索框（标题/摘要/标签）。

卡片：`story_time` 一行 · 等级色块（点按循环 S→A→B→C；长按钉/取消钉 S）· 标题 · 摘要（点开展开全文，
再点进入编辑 textarea）· 状态徽标（待处理/失败/过期/孤立/归档/兜底抠取）· 楼层号（点击跳到该楼）。
展开后操作：编辑保存 / 重摘要 / 删除 / 标记该楼不入库。

底部操作条：补齐（手动触发 ingest）· 对账 · 重试全部失败 · 导出 JSON / 导入 JSON · 清空（二次确认）。

失败可见性：有 `failed/refused` 条目时顶部红条常显，点击展开错误原因。

样式：只用酒馆的 CSS 变量（`--SmartThemeBodyColor` 等），适配深浅主题；手机全屏，`@media (min-width: 1001px)`（与酒馆 mobile-styles 断点对齐）时改为居中弹窗加遮罩，点遮罩或 Esc 关闭。

面板尺寸不能靠 `inset:0` / `bottom:0` 撑：酒馆 `style.css` 给 `html` 加了 `-webkit-transform: translateZ(0)`，fixed 元素以 `html` 为定位基准；手机端 `body` 又是 `position:fixed`，脱流后 `html` 高度为 0，面板会被压成只剩标题条（0.3.0 手机端实测症状）。必须像酒馆自家抽屉一样写死 `100dvh`（`100vh` 兜底）。底色也不能只靠 `--SmartThemeBlurTintColor`（部分主题半透明），打开时按 `--SmartThemeBodyColor` 明暗先垫一层实底；不用 `backdrop-filter`。

---

## 10. 设置项

§10 的表单全部放在面板「设置」页；扩展设置抽屉只留总开关 + 两个打开按钮 + 状态行。

| 键 | 默认 | 说明 |
|---|---|---|
| enabled | true | 总开关 |
| autoIngest | true | 自动补齐；关掉则只手动 |
| api.url / api.key / api.model | 空 | 副 API；「拉取模型」+ 下拉选择，「测试连接」按钮 |
| api.models | [] | 上次拉取到的模型列表缓存，刷新页面后下拉框仍可选 |
| api.temperature | 0.3 | |
| api.maxTokens | 900 | |
| api.timeoutSec | 60 | |
| fallback.url / key / model | 空 | 备用模型，可空 |
| contentWindow | 6 | 与 6🌸 minDepth 同步 |
| injectDepth | 6 | |
| maxInjectChars | 9000 | |
| ingestBatch | 20 | 单次补齐上限 |
| archive.enabled | true | 归档开关 |
| directive | 空 | 记忆导演指令，附加到摘要提示词 |
| debug | false | 控制台日志 |

密钥存在 `extension_settings`（酒馆 settings.json，本机文件）；不走 `/api/secrets` 以免占用 custom 槽位。

---

## 11. 设计取舍（为什么这样做）

- ~~逐楼一条，不做多楼批量提取。~~ **v0.3 推翻**：改为窗口制、按事件切条目，见 §15。逐楼是省对账代码的选择，不是质量考量。
- **输入只给正文 + recap + 用户行动。** 说戏 COT 含未采用的备选走向，状态栏是快照不是事件，道具卡是 HTML，三者进摘要都会制造假记忆。
- **send_date 定位 + 内容哈希判过期。** 删楼、编辑旧楼、开分支三种情况共用一套对账，不需要分别处理。
- **归档非破坏。** 原条目保留可恢复，合并段只是注入时的替身。
- **注入零网络。** MVP 的 interceptor 只做过滤和格式化，生成不会被记忆层拖慢；二期向量也只允许一次 embedding 调用且带硬超时。
- **锁在内存不在文件。** 切聊天、刷新页面自动失效。
- **等级 + 钉选 + 信息差三个字段是 Erato 场景的刚需。** 等级决定衰减与保底，钉选让用户纠正副 AI 的误判，`known_by` 对应预设指南里「角色未卜先知」那条排障项。
- **手机优先。** 全屏浮层、不用原生弹窗、只用酒馆 CSS 变量。

---

## 12. 分期

**v0.1 MVP**：§3–§7 + §9 基础面板（列表/编辑/钉S/删除/重摘要/补齐/对账/导出导入）+ §10 设置。
不含归档、正典压缩、备用模型（预算超出只做裁剪+提示）。

**v0.2 治理**：§8 全部 + 备用模型 + 疑似拒答自动换模。

**v0.3 向量召回**：embedding（走何种转发路径待查——酒馆 chat-completions 端点不做 embeddings，
候选是酒馆内置 `/api/vector/*` 或自建 server plugin）、S/A 保底 + B/C 相似度×权重竞争、related_to 链拉取。

---

## 13. 真机实测清单（v0.1）

（预设侧 📖预设使用说明 第六节的「长程剧情记忆」归宿一行，待插件跑稳后改为指向本插件。）

1. 装载：扩展面板出现、顶栏图标出现、无控制台报错。
2. 副 API：测试连接成功；填错密钥时错误可见。
3. 入库：新开一局聊到第 4 楼（chat.length ≥ 4），面板出现第 1 楼条目；说戏 COT 未混入摘要。
4. 抠取：故意在某楼删掉 `</content>`，条目带「兜底抠取」徽标且摘要正常。
5. 注入：聊到 depth≥6 出现，打开酒馆「提示词查看器」确认 `<erato_memory>` 块在 depth 6 且只含 depth≥6 楼层。
6. 对账：删一楼 → 对应条目变孤立；编辑一楼正文 → 变过期并自动重摘要；开分支 → 分支里越界条目变孤立。
7. 钉 S：钉后进正典区，改等级无效直到取消钉。
8. NSFW：用 🚗 组开着的楼层看副 API 是否拒答，拒答时红条可见。
9. 失败路径：断网发一楼 → 待处理 → 恢复后自动补齐。
10. 切聊天：A 局条目不出现在 B 局。

---

## 14. v0.2 定案（2026-09-02，v0.2.0 真机实测反馈后）

实测反馈：①顶栏图标在手机端把其他图标挤到边缘；②一百多楼的旧聊天打开面板一片空白，不知道点哪里是「总结前文」；
③用户实际用法是「总结完把旧楼藏起来，只留几楼在眼前」。
参考了另一款剧情总结插件的界面截图（桌面 111.png / 222.png），只抽原则不搬样式。

### 14.1 入口：悬浮球（已定）

- 顶栏图标**删除**，不留开关。入口 = 悬浮球 + 魔杖菜单「记忆面板」。
- 悬浮球：`position:fixed` 圆球，默认约 40px，默认停在输入框上方靠右；可拖拽（移动 >6px 才算拖，否则算点击），
  松手吸附最近的左右边缘，位置存 `extension_settings`，刷新后保持。
- 单击 = 打开面板；面板打开时球隐藏，关面板再出现。
- **插件总开关关 → 球不显示；开 → 显示**（跟随 `enabled`）。
- 设置项：`ball.enabled`（默认 true）、`ball.color`（默认取酒馆 `--SmartThemeQuoteColor`）、`ball.opacity`（默认 0.4，按下/异常时 1）、`ball.size`（32–56px，默认 40）。
- 球上角标：有未总结楼层显示数字；后台总结中球边缘转圈；有失败/拒答角标变红。
- 初始位置避开酒馆「滚到底部」按钮与 QuickReply 条：右侧、输入框上方约 1.5 球径。
- 魔杖菜单项文字带状态：「记忆面板」/「记忆面板 · 3 条失败」/「记忆面板 · 处理中」；另加一项「总结前文 · 尚有 N 楼」直接开跑不开面板。
- 失败第一次出现弹一次 toastr，不重复弹。

### 14.2 「总结前文」：一个动词，一次跑到底

- 面板原「补齐」按钮改名「总结前文」，放顶部主按钮位；列表为空时显示引导「本聊天有 N 楼尚未总结，点上方按钮开始」。
- 手动模式：一次跑到所有待总结楼层完成，按钮变「停止」可中断，旁边显示进度「12/87」；跑前弹确认框显示楼数（防误点千楼聊天烧费）。
  不设楼数上限。进度 toast 每 10 楼刷一次，完成报「已总结 N 楼，注入 ≈ M 字」，失败楼数单独一条 warning。
- 自动模式（聊天进行中）保持每次最多 `ingestBatch`（20）楼，静默。
- **隐藏楼层判定修正（bug）**：酒馆隐藏消息 = `is_system:true`。现行 `pendingFloors` 对 `is_system` 一律跳过，
  导致藏起来的旧楼永远不入库。改为：`is_system && !is_user && (name === ctx.name2 || 能抠出 <content>)` → 视为 AI 楼层照常总结；
  只跳过真正的系统消息。

### 14.3 隐藏已总结楼层（已定，默认勾上）

- 设置 `hideSummarized`（默认 **true**）+ `keepVisible`（默认 **6**，三回合，与预设「最近三回合全文」口径一致）。
- 总结批次结束后：所有 `status===ok` 且楼层号 < `chat.length - keepVisible` 的 AI 楼层及其前一条用户楼层标 `is_system=true`（酒馆隐藏消息的做法），
  调 `saveChat`。只隐藏已总结成功的楼，失败/待处理的楼不藏。
- 「更多」里有「取消隐藏」一键恢复：把本插件隐藏过的楼层（记在 `data.hidden: [send_date]`）全部 `is_system=false`。
  只恢复自己藏的，不碰用户手动藏的。
- 注入窗口改按**可见楼层**计算：注入所有已隐藏楼层的条目 + 可见楼层中深度 ≥ `contentWindow` 的条目。
  隐藏后可见楼全是全文、记忆块在它们前面，中间不再有 recap 层；预设侧 5🌸 minDepth 改 6 的可选项作废。
- 这是本批唯一改动聊天文件楼层状态的功能，可逆，用户已明确同意默认开。

### 14.4 面板重排（手机单列）

从上到下：
1. 标题 + 三个大数字：已记录事件 / 已总结楼层 / 待总结；有失败时多出红色第四个。原状态长句收进展开。右上角关闭。
2. 操作行：「总结前文」主按钮（运行中变「停止」+ 进度）；「隐藏已总结 · 保留 [6] 楼」勾选；「更多」下拉（重试失败 / 导出 / 导入 / 取消隐藏 / 清空 / 设置）。
   「对账」不再有按钮，切聊天与删楼时自动跑。
3. 「人物与物件」区（可折叠）：见 §14.5。
4. 「时间线」区：现有条目列表。视觉改为：标题 + 故事时间一行、摘要一段、人物与标签一行、楼层号在末尾（点击跳楼）。等级色块 / 钉 S / 筛选 / 搜索保留。
5. 「本轮注入预览」（可折叠）：显示上一次 `buildBlock()` 的原文，替代手机上难用的提示词查看器。
6. 手动新增：条目 / 人物 / 物件三种，补录遗漏用。
7. 设置：不再是同级 tab，从「更多」或齿轮进入，覆盖层；分组 = 总结 / API / 悬浮球 / 提示词 / 调试。
   「提示词」组可查看并编辑完整摘要提示词模板（原「导演指令」升级），留「恢复默认」。
8. 斜杠命令：`/em-panel`、`/em-summarize`、`/em-pin <楼层号>`、`/em-note <文字>`，供 QuickReply 接入。

### 14.5 人物与物件档案（已定，第二批）

- 形态：**字段 + 值 + 来源楼层号**，不是一段简介。例：「公开身份：渝州市委副书记 #20」「位置：归原酒店 #72」「旧牛皮笔袋：在陆昭宁手里 #45」。
- 产出：现有逐楼摘要 JSON 加 `entities` 字段，同一次调用顺手报本楼出现的配角/物件及其新信息，**零额外请求**。
- 合并规则：按（实体名, 字段）为键，新楼层非空值覆盖旧值并更新来源楼层号；面板每个值可见「#楼层」，点击跳楼核对；值可手改可删。
- 主角（`name1/name2`）不自动建档（卡与世界书已有），只建 NPC 与物件；用户可手动为主角建卡。
- 人物字段：名字、别称、身份/职业、年龄、与{{user}}关系、与{{char}}关系、外貌一句、当前立场、现状（在场/离开/死亡）、首次登场楼、知情范围。
  物件字段：名字、持有者、状态、意义、首次出现楼。不做关系图，关系用两个「与…」字段文字表达。
- 注入：同一 `<erato_memory>` 块内，在「正典」前加「## 人物志」「## 物件」两节，每张卡压成一行 ≤80 字；
  独立预算上限 1500 字，超出丢最久未露面的。
- 对账：实体每个值记来源 send_date；来源全部孤立 → 该值标孤立；分支越界 → 标待核，不自动删。
- 同批：**按故事日期的大纲**替代 §8.2 的「相邻 8–12 条合并」：每条记忆的 story_time 自带日期，按天分组，
  副 API 生成一天 60–100 字大纲段，非破坏、常驻注入、不参与预算裁剪；被裁掉的 B/C 在大纲里仍留一句。
- 同批：「追溯整理」多楼合一调用（5 楼一次）省钱，但输出仍逐楼一条，与 send_date 定位/对账完全兼容。
- 同批可选：副 API「渠道」下拉（OpenAI 兼容 / Claude / Gemini），走酒馆后端对应 `chat_completion_source` 的 reverse_proxy，需真机验证。

### 14.6 从参考插件明确不借的

跨聊天存档系统（已定按聊天隔离，IF 线用导出/导入）、自然语言实时指令（副 AI 调工具改数据）、
从世界书提取档案（世界书是权威，不反向抄一份进记忆）、人物关系图。

### 14.7 向量召回：本轮不做（建议，待用户拍板）

见对话结论：第一、二批落地并真机跑过后，若条目总量仍在预算内则不需要；
第二批先用**关键词优先**（用户最新输入与条目 tags/characters 匹配，命中者在预算裁剪时优先保留）作零网络、零存储的替代，
覆盖向量的主要收益「该露哪几条 B/C」。向量的前置问题（embedding 转发路径 / 存储位置 / 生成前延迟 / NSFW 拒嵌）留到 v0.3。
**0903 v0.3.2 实测通过后用户拍板启动向量，四个前置问题的答案见 §17。**

### 14.8 分批

- **第一批 v0.2.1**：§14.1 悬浮球 + §14.2 总结前文与隐藏楼层判定修正 + §14.3 隐藏已总结 + §14.4 面板重排（含注入预览、手动新增、斜杠命令）。
  全在入口/面板/入库判定层，不改摘要提示词与条目结构。
- **第二批 v0.2.2**：§14.5 人物物件档案 + 按日大纲 + 追溯整理多楼合一 + 关键词优先 + 渠道下拉（可选）。
- **v0.3**：向量召回（若仍需要）。

---

## 15. v0.3 窗口制定案（2026-09-03）

讨论起点：v0.2 每个 AI 楼层各调一次副 API。一场跨十楼的争吵或一段亲密戏会产出十条近似条目，
「最近三条记忆」去重在第五楼以后就失效；请求数、输出 token、注入预算三头浪费。
§11 第一条「逐楼一条」当初是为了省对账代码，不是质量考量，本节推翻它。
参考了另一款剧情总结插件的做法（按消息数攒批、事件级输出、NPC 档案 + 关键词激活），只借机制不搬样式。

### 15.1 窗口 = 入库与对账的单位

- `Window = { id, floors[], dates[], hashes[], fallback, status, attempts, last_error, model }`：一次副 API 调用覆盖的楼层段。
  `dates` 是 send_date 列表（跨删楼定位），`hashes` 是每楼 `<content>` 的哈希（判编辑）。
- 记忆条目（事件）挂在窗口下：`Entry.win = 窗口 id`，`Entry.src = { idx: 末楼, floors[], dates[], fallback }`，
  副模型为每个事件标明来自哪几楼，跳楼核对、信息差、等级全部保留。一窗产出 1–N 条，按事件切不按楼切。
- 覆盖表：楼层的 send_date 在任一非孤立窗口里 = 已入库。未入库楼层 = AI 楼、未标不入库、不在覆盖表里。
- 对账：窗口内任一楼被编辑 → 窗口 `stale`，整段重跑（条目状态镜像窗口）；部分楼被删 → 窗口缩到剩下的楼并 `stale`；
  全删 → `orphan`；分支切回来哈希吻合 → 恢复 `ok`。手动条目不变。
- 隐藏已总结楼层改按「ok 窗口覆盖的楼层」判定，其余规则同 §14.3。
- 迁移：v1 数据每个自动条目变成单楼窗口（`migrateV1`），失败占位条目丢弃但窗口保留其失败状态；导入旧导出文件同样迁移。

### 15.2 两个楼数设置

| 设置 | 含义 | 默认 | 取值 |
|---|---|---|---|
| `autoInterval` 自动总结间隔 | 攒够这么多 depth≥2 的未入库楼自动跑一次 | 20 | 0 = 手动；快捷 20/30/40；可手输任意 |
| `windowFloors` 单次总结最大楼层 | 一次副 API 调用最多吃多少楼 | 40 | 快捷 40/60/100/200；可手输任意 |
| `maxCallChars` 单次最大字数 | 一次调用材料字数上限，超过自动再切 | 60000 | 按副模型上下文调 |

- 两个楼数与面板上的「N 楼」自 v0.3.2 起都按酒馆楼层计，见 §16。
- 两者独立：自动触发时把未入库段按 `windowFloors` / `maxCallChars` 先到者切块；「总结到当前」不看间隔，从头到**最新楼**全部切块跑到底
  （含 depth 0/1；最新楼之后被 swipe 会让该窗口 stale 重跑一次，这是手动模式的已知代价）；自动模式仍只取 depth≥2。
- 中间隔着已入库楼层时断开，不把不相邻的楼拼进一个窗口。正文为空的楼直接标不入库。
- W 越大越省钱也越粗（200 楼压成一次调用副模型只会给十几条事件）：日常 40–60，清不在乎细节的老积压再开 100–200。
- 拆半重试：拒答、JSON 解析失败、无事件/全部过短 → 窗口对半拆成两个新窗口插回队首，切到单楼还失败才标 `refused/failed`；
  网络/超时错误不拆，只计次数（上限 3）。大窗口不再一次废掉几十楼。
- 副 API 默认 `maxTokens` 4000、超时 120s（旧默认 900/60 自动抬）。旧模板不含 `{{material}}` 自动清空退回默认。

### 15.3 人物志 / 物件 / 关系现状（§14.5 落地）

- 同一次调用顺手输出，零额外请求。JSON 顶层：`events[]`、`relation`、`people[]`、`items[]`。
- `people`：`{ name, aliases[], f: { role, age, rel_user, rel_char, look, stance, status, knows }, first_idx, last_idx }`，每个值 `{ v, idx, date }` 带来源楼层号；
  `items`：`{ name, f: { holder, status, meaning }, first_idx, last_idx }`。合并规则：按名字/别称匹配，新的非空值覆盖并更新楼层号；主角 `name1/name2` 不建档。
- **主角关系现状**（用户定案「要」）：`relation = { v, idx, date }` 一行，副模型每窗给一句 `{{char}} 此刻对 {{user}} 的态度与关系`，本段无变化留空不覆盖。
  攻略型角色从陌生/敌对到恋人是动态的，卡里写的只是初始值。
- 注入：`<erato_memory>` 内，正典之前依次「## 关系现状」「## 人物志」「## 物件」。人物志按**关键词激活**：最近 `npcScanDepth`（6）条可见消息里提到名字/别称的出完整卡，
  其余压成一行「其他已登场：名·身份」；档案总预算 `entityChars`（1500），放不下的完整卡按最久未露面降级。§14.7 的「关键词优先」以此落地，向量继续不做。
- 面板新增「人物与物件」分区：关系现状可直接改；人物/物件卡点开可改名字、别称、各字段，可删；每个值旁的 `#楼层` 可点跳楼。
- 提示词把已有档案名单和上次关系现状喂回去，要求名字与档案一致，避免同一人两个名字。

### 15.4 面板与命令

- 主按钮「总结前文」改名「总结到当前」；确认框报楼数与约多少次调用。魔杖菜单与 `/em-summarize` 同步改名。
- 失败/拒答/排队中的窗口在时间线顶部显示为一张段落卡：重试 / 拆半重试 / 本段不入库。条目卡的「重摘要」改为「重跑本段」（替换该窗口全部条目）。
- `/em-pin 楼层号` 找的是包含该楼的事件条目。
- 设置页「总结节奏」组：自动总结下拉 + 自定义楼数、单次最大楼层下拉 + 自定义楼数、单次最大字数；注入组加档案上限与扫描楼数。

### 15.5 未做 / 已知限制

- 档案值的来源楼被删或分支越界不自动标记（§14.5 原定「标孤立/待核」），先看实测是否需要。
- 手动改过的档案值/关系行会被后续窗口的新值覆盖（新值非空即覆盖），没有「锁定」；需要时加。
- 按日大纲（§14.5 同批）与归档合并仍未做；窗口制把条目数压下来之后，是否还需要待实测。
- 手动「总结到当前」不检测生成中：生成过程中点会把半截楼层吃进去（哈希会变 → 之后 stale 重跑，能自愈）。

---

## 16. v0.3.2 楼数口径改为酒馆楼层（2026-09-03）

起因：114 楼的聊天面板显示「待总结 58 楼」。v0.3.1 及之前面板与设置里的「楼」都只数 AI 楼，用户一条 + AI 一条算 1，
而同一状态行里的「已隐藏 N」数的却是实际消息数，两种口径混在一起容易误解。

- **对外一律按酒馆楼层计，对内存储不动**。`windows.floors`、`entries.src.idx`、覆盖表、隐藏逻辑仍以 AI 楼为基准，旧数据零迁移。
- 跨度 `floorSpan(chat, idx)`：一个 AI 楼 = 它自己 + 紧挨着前面那条用户楼（材料就是这么配对的，隐藏也是一起藏）= 2；
  开场白、续写出来的连续 AI 回复前面没有用户楼 = 1。用户楼后面跟的是旁白（`extra.type`）时那条用户楼不计，与旁白本身一样不入库。
- 换算点：`counts()` 的已总结/待总结/已攒、`planWindows` 的 `windowFloors` 上限、自动触发的 `autoInterval` 比较、
  进度条与完成提示、「总结到当前」确认框、失败段落卡与重跑/不入库确认。楼**号**（`#12`、`#30–#49`）本来就是 `chat[]` 下标，不变。
- 设置默认值翻倍保持实际行为不变：`autoInterval` 10→20，`windowFloors` 20→40；快捷档 20/30/40 与 40/60/100/200。
  旧设置加载时按 `floorUnit` 标记一次性翻倍（0 = 手动保持 0；v0.2 直升没有这两项的用新默认，不翻倍）。
- 提示词里的 `{{floor_count}}` 仍是材料块数（每块 = 一个 AI 楼及其用户行动），不改，避免扰动副模型行为。

---

## 17. v0.4 向量召回草案（2026-09-03，待用户拍板，未写代码）

前提：v0.3.2 真机实测通过（115 楼待总结 = 开场白 1 + 57 对 × 2，口径正确；手机端铺满、电脑端居中）。
用户定案启动向量。本节只回答 §14.7 留下的四个前置问题并给出机制，不重开已定案项。

### 17.1 向量解决什么、不解决什么

- 只做一件事：**把顺序注入放不下、被预算裁掉的条目，按「当下在聊什么」捞回来**。S/A 顺序注入、正典、人物志关键词激活全部不动。
- 条目总量在 `maxInjectChars` 之内时（约 200 楼以下）召回永远为空，行为与 v0.3 完全一致。
- 顺带作废两项未做项：§8.2 归档合并、§14.5 按日大纲。它们的目的都是「把老 B/C 压小以便留在窗口里」，有召回之后老条目不需要留在窗口里，掉出去也能回来。
- 不做：用向量替代顺序注入（时间线会乱）、向量化原始楼层（酒馆自带的 Vector Storage 已经做这个，而且会把说戏 COT 一起嵌进去）、跨聊天召回。

### 17.2 嵌入转发路径（已从酒馆 release 源码核实）

酒馆后端自带 `/api/vector/insert | query | query-multi | list | delete | purge | purge-all`，请求体 `{ collectionId, source, ...源参数 }`：

- `insert`：`items: [{ hash: Number, text, index }]`，后端算向量后 upsert，元数据存 `{hash,text,index}`。
- `query`：`{ searchText, topK, threshold }` → `{ metadata: [{hash,text,index}], hashes }`。**只用 `metadata`**（`hashes` 没过阈值筛）。
- `list` → 该集合全部 hash；`delete`：`{ hashes }`；`purge`：`{ collectionId }`。
- 存储位置：服务端 `data/<user>/vectors/<source>/<collectionId>/<model>/`（vectra 本地索引）。**浮点不进 chat_metadata**（§3 的要求满足）；换 source 或 model 自动落到新目录 = 自动重建，不会混用不同模型的向量。
- `hash` 必须是数字：沿用酒馆 `getStringHash`（32 位整数）算法，插件内实现同款。

可用的 `source`（按推荐顺序）：

| source | 密钥从哪来 | 地址/模型 | 备注 |
|---|---|---|---|
| `siliconflow` | 酒馆密钥槽 SILICONFLOW | 固定官方地址，`siliconflow_endpoint:'cn'` 走国内站；默认模型 `Qwen/Qwen3-Embedding-0.6B` | 中文强、便宜；**需酒馆版本含此 source**（release 分支已有，用户酒馆需核对版本） |
| `vllm` | 酒馆密钥槽 VLLM（Bearer） | 请求体 `apiUrl` + `model`，后端打 `${apiUrl}/v1/embeddings`，标准 OpenAI 格式 | **通用路线**：任何 OpenAI 兼容 embeddings 地址都能塞进来，插件自己传 url/model，不碰酒馆向量扩展的设置 |
| `openai` | 酒馆密钥槽 OPENAI | 固定官方地址，请求体 `model` | 需能直连 OpenAI |
| `transformers` | 无 | 服务端本地 ONNX，模型由 config.yaml `extensions.models.embedding` 决定 | **不推荐**：手机 Termux 跑推理慢，默认模型英文 |

密钥写入：酒馆前端 `writeSecret()` 走 `/api/secrets/write`，插件设置页放「写入密钥到酒馆」按钮（vllm 源写 VLLM 槽、siliconflow 源写 SILICONFLOW 槽），并提示该槽位会被覆盖。`getContext()` 若未暴露 `writeSecret`，退化为引导用户到 API 连接面板手填。

### 17.3 向量化什么、何时向量化

- 单位 = 条目（Entry）。`text = story_time｜title｜summary｜情绪：emotion_shift｜人物：characters`，≤300 字；`index = src.idx`；`hash = getStringHash(text)`。
- `Entry.vec = { hash, at }`；`data.vector = { source, model, collection, builtAt }`。`collectionId = 'erato-mem-' + getStringHash(chatId)`，与酒馆自带向量扩展的集合名（chatId 本身）不撞。
- 时机：**窗口入库成功后**（`ingest` 末尾）把该窗口的新条目一次 `insert`；失败不阻塞入库、不标窗口失败，条目 `vec=null`，下次 `ingest` 或面板「补向量」重试。
- 条目编辑保存 → `delete` 旧 hash + `insert` 新；删条目 / 重跑本段 / 窗口 `orphan` → `delete`；清空 → `purge`。
- 对账：切聊天时 `list` 一次，与本地 `ok` 条目做差集：本地有向量没有 → 补 insert；向量有本地没有 → delete。幂等，与 §4.2 同一处执行。
- 人物志/物件不向量化（值会变，关键词激活够用）；手动新增条目同样向量化。

### 17.4 查询与生成前延迟

- 位置：`eratoMemory_intercept`（`type !== 'quiet'`）。这是 §11 已定的「唯一一次 embedding 调用且带硬超时」。
- 查询串：最新用户输入（去 OOC 括号）+ 最近一条 AI 楼的 `<recap>` 叙述（不是正文，正文有几千字会稀释话题），合计 ≤ `vector.queryChars`（600）。
- 参数：`topK` 8、`threshold` 0.4（Qwen3-Embedding 余弦，待实测调）、硬超时 `vector.timeoutSec` 4 秒（AbortController）。超时/报错 = 本轮无召回，注入块照旧，**绝不 abort 生成**；面板状态行显示「召回失败：原因」。
- 缓存：查询串 hash → 结果，swipe / 重生成不再调用；切聊天清空。
- 手动模式「总结到当前」期间不查询（材料还没入库）。

### 17.5 召回结果怎么进注入块

- 候选过滤：`status === 'ok'` + `injectable`（depth≥6 规则同样适用，避免与正文重叠）+ **不在本轮已注入的正典/往事里**（只捞被裁掉的）。
- 预算 `vector.recallChars` 默认 2000，独立于 `maxInjectChars`。超出按分数低者丢。
- 输出顺序按 `src.idx` 升序而不是按分数，让主模型保持时间感。格式与往事同款 `fmtEntry(e, true)`。
- 位置：`## 往事` 之后新增：

```
## 相关往事
[按当下话题从更早的记忆中召回，时间早于上面的往事，同样只作已发生事实]
- {story_time}「{title}」({grade}) {summary}｜情绪：…｜知情：…
```

- 面板注入预览加一行「召回 N 条 ≈ M 字」；`lastInject` 加 `recalled`。

### 17.6 设置项（新增 `vector` 组，全部在面板设置页「向量召回」组）

| 键 | 默认 | 说明 |
|---|---|---|
| vector.enabled | **false** | 总开关；关闭时不查询、不插入，已有向量保留 |
| vector.source | `siliconflow` | 下拉：siliconflow / vllm / openai |
| vector.apiUrl | 空 | 仅 vllm 源用 |
| vector.model | 按源默认 | siliconflow 默认 `Qwen/Qwen3-Embedding-0.6B` |
| vector.cn | true | siliconflow 国内站 |
| vector.topK / threshold | 8 / 0.4 | |
| vector.recallChars / queryChars | 2000 / 600 | |
| vector.timeoutSec | 4 | 查询硬超时 |

按钮：「测试连接」= insert 一条探针 → query → delete；「重建向量」= purge + 全量 insert（显示进度，可停）；「补向量」= 只补 `vec=null` 的条目。

### 17.7 与酒馆自带 Vector Storage 扩展的关系

互不干扰：不同 collectionId、不读不写它的设置。但**建议用户不要同时开它的「聊天消息向量化」**，否则原楼层（含说戏 COT、状态栏）会被它重复注入，与 §11「输入只给正文 + recap」冲突。

### 17.8 NSFW 拒嵌

embedding 接口一般不做内容审核（硅基 Qwen3-Embedding 实测待补），若某条 insert 被拒：该条 `vec = { error }`，面板显示「N 条未向量化」，不影响顺序注入。

### 17.9 待拍板

1. 默认 source：推荐 `siliconflow`（需先看用户酒馆版本是否含该源；不含则默认 `vllm` 通用路线）。
2. `threshold` 0.4 / `topK` 8 / `recallChars` 2000 三个默认值。
3. 归档合并与按日大纲正式作废（§17.1）。
4. 召回是否也用来激活人物志完整卡（建议二期，先看条目召回效果）。
5. `vector.enabled` 默认关、用户手动开。

### 17.10 实测清单（v0.4）

- 硅基 / vllm 两条路线各测一次「测试连接」；密钥写入按钮是否可用。
- 200 楼以上聊天：裁掉条目数 > 0 时，聊到早期人物/物件，「相关往事」是否召回正确条目；无关话题时是否为空（阈值）。
- 手机弱网：查询超时是否 4 秒内放行、生成不受阻。
- swipe 不重复调用；切聊天对账差集是否正确。
- 「重建向量」在 100+ 条时的耗时与进度。

---

## 18. 人物志漏人问题：根因与提示词重做草案（2026-09-03，待拍板，未写代码）

实测现象：一个 100 多楼的聊天「总结到当前」后，人物志只有极少数人，若干重要角色没有建档。

### 18.1 根因（代码证据）

1. **尾部静默截断。** `callApi` 不看 `finish_reason`；输出被 `max_tokens`（默认 4000）切断后，`parseJson` 的 `repairTruncated` 会把残缺 JSON 补闭合再解析成功。
   输出 JSON 的顺序是 `events → relation → people → items`，40 楼窗口通常产出 8–12 条事件、每条十来个字段，
   仅 events 就逼近 4000 token，排在尾部的 `people/items` 被整段切掉，而窗口状态照样是 `ok`，面板不报任何异常。
   这是「人物极少」的第一嫌疑：不是模型没写，是写了没落地。
2. **提示词让模型「少写人」。** 模板写的是「people 只填本段出现了新信息或有变化的」，同时要求每人 8 个字段。
   模型在事件已经很重的情况下，对「要不要给这个人开一条」做的是人级取舍：嫌重就整个人跳过，而不是字段级留空。
   模板里没有任何一步要求「把本段出现过的人先点一遍名」。
3. **{{char}} 混淆。** 模板写「对手角色：{{name2}}」「配角名（不含 {{name1}} 与 {{name2}}）」，`mergeEntities` 也跳过与 `name2` 同名的人。
   若卡是旁白/世界/多角色卡，模型会把最重要的那个 NPC 当成 {{char}} 本人而不建档。**需用户核对：缺席的角色名是否正好与卡名/卡内主角相关。**

次要：`events[].characters` 里已经有每个事件的在场者名单，但从未反哺人物志，白白浪费了一份现成的点名表。

4. **亲属类角色的两个漏网点（0903 用户核对：缺席的是 {{user}} 的亲属，属重要角色，且原文明确写了名字）。**
   - 亲属常常只被提到、不在场（回忆、电话、转述）。events 的 `characters` 定义是「在场且有行动的人」，被提到的人进不去；people 的字段说明写的是「配角名」，模型容易把不在场的家人排除在「配角」之外。
   - 模型倾向把 {{user}} 的家人当成 {{user}} 人设的一部分（背景资料）而不是独立 NPC，而模板没有一句话否定这个倾向。
   「没有正式名」**不是**本次原因（名字在原文里明写），B2 的规范名规则只作通用健壮性保留。第 3 条（{{char}} 混淆）本次不成立，`charIsNarrator` 降为可选项。

**以上第 1、2、4 条都还是假设，需要用数据分辨**（见 18.6 第 0 项）：截断能从导出的 JSON 里直接看出来，提示词问题则要看窗口产出的 events 里有没有这些亲属的名字。

### 18.2 对参考提示词的逐段解读与取舍（只借机制，不搬文字、不提出处；0903 二稿）

参考版增量总结提示词的六个部件，各自解决的问题与我们的对应：

| 部件 | 它解决什么 | 我们的现状 | 取舍 |
|---|---|---|---|
| 前置清点（输出 JSON 第一个键是「本轮新增了什么、已有 X 识别 Y」） | 逼模型先盘点再动笔，便宜的强制计数 | 无 | **借**：cast 点名表放 JSON 首位，措辞写成「先清点本段出场的人，再写事件」 |
| 新角色独立清单（只报名字） | 建档与详细档案脱钩，零成本 | people 一上来就要 8 字段 | **借**：即 cast |
| participants 只用正式人名、禁代词别名 | 同一人在所有事件里名字一致，便于聚合 | characters 无此约束，合并靠精确匹配 | **借**：写进 characters 字段说明 |
| summary 召回导向（合格/不合格例句对照，保留原名原称呼地点物件动作） | 未来一句口语提法也能认出这条 | 我们偏因果「写为什么」 | **借并合并**：因果 + 可识别两条都要 |
| 事实三元组，尤其 `p=对X的看法 + trend` | 任意两人之间的态度可记录、可覆盖 | 只有 NPC→{{user}}、NPC→{{char}} 两根线 + {{char}}→{{user}} 一行 | **借结构不借形式**：人物志加「对他人的看法」多行字段（见 18.3 H）；不另建三元组库 |
| 弧光（trajectory 一句 + progress 数字 + newMoment） | 按人维护成长线 | 只有按事件的 emotion_shift | **借一半**：按人一句「当前阶段」，不要数字（见 18.3 I） |

不借：多轮假对话的角色帽子（无信息量）、八类型五权重（type/grade 已定案）、全局关键词表（tags + §17 向量已覆盖）。

参考版聊天历史里夹着的「怎么截断了！重新完整生成……3000 字以内」是它内置的截断重试，说明这类结构化输出撞上限是常态。
**借**：截断后先带长度约束重来一次，仍截断再拆半（见 18.3 D）。

参考版记忆注入提示词四行的对应：

- 「以上是还留在眼前的对话，以下是记忆」——边界声明。我们只说了「覆盖最近三回合之前」，**借**这句交代方式。
- 「定了的事 = 硬性约束」= 我们的正典。
- 「其他人的事 = 当前角色未必知晓，遵守信息墙」= 我们的知情栏；它写在块首，我们写在每条末尾。**借**：块首加一句总则，每条末尾保留。
- 「事件可回调、呼应、形成对比」已借（G）；「弧光 = 关系阶段参考」对应 I。

### 18.3 改法（我们自己的）

**A. 输出顺序重排：点名表放最前。** 新 JSON 顶层顺序 `cast → events → relation → people → items`。
`cast` 是本段**每一个**有名字或固定称呼的人，包括只被提到、包括主角（代码过滤），一人一行，宁多勿漏：

```json
"cast": [
  {"name": "正式名", "aliases": ["原文里的称呼/昵称/别称"], "tier": "主 | 配 | 龙套", "role": "一句身份", "seen": [楼层号]}
]
```

截断先切尾巴，点名表在头部一定保得住；`tier` 只用于面板排序与注入预算（完整卡优先主/配，龙套不进注入块）。

**B. 建档与更新分离。** `people` 改为「本段有新信息的人的字段更新」，明确一句：
「没变化的人不必出现在 people 里，但必须出现在 cast 里」。把「少写」精确到字段级，不再让模型做人级取舍。
八个字段全部「有则填、无则不写该键」，不再要求空字符串占位。

**B2. 没有正式名的人也要建档。** cast/people 的 `name` 允许是称呼，规范写法为「{{name1}}的母亲」「{{name2}}的舅舅」这种「谁的什么人」；原文里的叫法（妈、老妈、王阿姨）全部进 aliases。
后续原文给出真名时，模型在 people 里输出 `{"name": "真名", "aliases": ["{{name1}}的母亲", …]}`，合并逻辑按 aliases 命中旧档案后把 `name` 升级为真名（现有 `findPerson` 已按别称匹配，只需加「命中后可改名」）。
模板明文：「{{name1}} 与 {{name2}} 的亲属、朋友、同事、上司一律作为独立人物建档，即使只在对话或回忆里被提到、从未到场；不要把他们当成主角人设的一部分。」

**C. 事件反哺。** `applyWindowResult` 把 `events[].characters` 与 `cast` 合并：凡非主角名字一律建档（至少 name / first_idx / last_idx / 登场段数），
零额外调用。已有聊天不重跑也能从现有条目立刻补出名单（面板「从事件补人物」按钮，零网络）。

**D. 截断不再静默。**
- 读 `choices[0].finish_reason`：`length` → 先带长度约束重来一次（追加一条 user 消息：「输出被截断，重新输出完整 JSON，events 的 summary 各压到 80 字以内，不要任何其他文字」），仍 `length` 再走拆半（与拒答同路，不算失败）。
- `repairTruncated` 生效且原文没有 `"people"` 键 → 窗口标 `partial`，面板段落卡显示「人物志可能不全 · 重跑本段」。
- `max_tokens` 改为按窗口动态给：`min(上限, 600 + 事件上限×400 + cast 预估×120)`，默认上限 8000；设置里的 maxTokens 变成上限而不是固定值。

**E. {{char}} 处理（可选项，本次实测不是原因）。** 新增设置 `charIsNarrator`（默认关）：开启时 `name2` 不再被跳过、模板改写为「{{name2}} 是叙述者/多角色卡，卡内所有角色都要点名建档」，
关系现状行改为「对 {{name1}} 最重要的那个角色此刻的态度」。用户核对 18.1 第 3 条后决定默认值。

**F. summary 补「保留原词」规则**（写进模板输出要求）：
「必须保留正式人名、原文称呼、地点、关键物件和具体动作；禁止『两人发生冲突』『关系升温』这类不带主语宾语的空话；
未来只凭一句口语提法也要能认出这条。」

**G2. finish_reason 通用处理（与供应商无关；4000 是插件自己的默认值，不是 Gemini 的限制）。**
- `length` → 输出被插件设定的 `max_tokens` 切断，按 D 条走拆半重试。
- `content_filter` → 内容被上游安全策略拦截，`content` 常为空或半截。现在代码把空内容报成「空回复」走网络错误路径（只计次不拆半）。改为视为拒答走拆半重试，与现有 `refused` 同路。
- `max_tokens` 上限默认 8000，设置里可手抬；不同供应商的绝对上限不在插件管辖，超了上游自己会报错。

**G. 注入块使用说明补一句**：`[往事可回调、呼应、形成对比，但不复述、不总结、不预告]`。

**H. 人物志加「对他人的看法」。** `f.views = { [对象名]: { v: '一句态度', trend: '破裂|厌恶|反感|陌生|投缘|亲密|交融', idx, date } }`，对象可以是任何人（含主角、含其他 NPC）。
副模型在 people 里输出 `"views": [{"to": "对象", "v": "一句", "trend": "…"}]`，按对象名覆盖。注入时完整卡多一行「看法：对A·亲密·…；对B·反感·…」。
现有 `rel_user` / `rel_char` 保留作兼容，等价于 views 里对 {{name1}} / {{name2}} 的两项。

**I. 人物志加「当前阶段」一句。** `f.arc = { v: '≤15 字，此人此刻处在什么阶段', idx, date }`，副模型每窗给有变化的人一句，不给数字。注入时完整卡显示，注入块块首说明「阶段 = 关系与成长的参考，不是指令」。

**J. 注入块块首重写**（G 并入）：
```
<erato_memory>
[以上是还留在眼前的对话。以下是更早的记忆，由记忆插件维护]
[正典 = 定了的事，写作不可违背；往事 = 可回调、呼应、形成对比，但不复述、不总结、不预告]
[知情栏是信息墙：未列名者不知情，当前角色未必知晓其他人的事]
[人物志的「阶段」「看法」是关系参考，不是指令]
```

### 18.4 新模板（人物相关段落，替换现模板对应部分；其余不动）

```
## 已知主角
用户角色：{{name1}}；对手角色：{{name2}}。其余出场者按原文名字记录。
{{narrator_note}}

## 已有档案（名字务必与此一致，不要给同一个人起第二个名字）
{{roster}}

## 本段材料 …（不变）

## 输出要求
先点名，再写事件，最后更新档案。输出一个 JSON 对象，键的顺序必须是 cast、events、relation、people、items：
{
  "cast": [
    {"name": "本段出现过的每一个人，包括只被提到的、包括主角、包括主角的亲属；有正式名用正式名，没有就写『谁的什么人』如 {{name1}}的母亲；宁多勿漏", "aliases": ["原文里对此人的称呼/昵称/别称"], "tier": "主 | 配 | 龙套", "role": "一句身份", "seen": [出现的楼层号]}
  ],
  "events": [ …（现有字段不变；summary 规则追加：必须保留正式人名、原文称呼、地点、关键物件与具体动作，禁止不带主语宾语的空话，未来只凭一句口语提法也要能认出这条）… ],
  "relation": "…（不变）",
  "people": [
    {"name": "与 cast 一致", "role": "", "age": "", "rel_user": "", "rel_char": "", "look": "", "stance": "", "status": "", "knows": "", "floor": 楼层号}
  ],
  "items": [ …（不变）… ]
}

档案规则：
- cast 是点名表：本段每一个有名字或固定称呼的人都要在，包括只被提到、没到场的。漏一个人比多写十个龙套更糟。
- {{name1}} 与 {{name2}} 的亲属、朋友、同事、上司一律作为独立人物建档，即使只在对话或回忆里被提到、从未到场；不要把他们当成主角人设的一部分。
- 没有正式名的人写「谁的什么人」（如 {{name1}}的母亲），原文叫法放 aliases；之后出现真名就用真名并把旧写法放进 aliases。
- people 是更新表：只写本段有新信息的人，字段有则填、没有就不写这个键。没变化的人不必出现在 people 里，但必须出现在 cast 里。
- 已有档案里的人，名字要与档案一模一样；同一个人在原文里的新称呼放进 aliases。
```

`{{narrator_note}}`：`charIsNarrator` 开时为「{{name2}} 是叙述者/多角色卡，卡内所有角色都按普通角色点名建档，不要把任何人当成 {{name2}} 本人。」，关时为空。

### 18.5 对已有聊天的补救顺序

1. 零网络：面板「从事件补人物」，用现有条目的 `characters` 建 stub（name + 楼层），立刻把名单补齐。
2. 一次轻量调用：「重建人物志」= 只带 cast + people 两个输出键的精简模板，按 60 楼一窗跑，不动事件条目。
3. 之后新窗口走新模板，自然覆盖。

### 18.6 核对结果与待拍板

已核对（0903）：
1. 缺席的是 {{user}} 的亲属，原文明写了名字 → 排除「无正式名」与「{{char}} 混淆」；剩 18.1 第 1 条（截断）、第 2 条（提示词让模型少写人）、第 4 条（不在场/被当背景）三个假设。
2. 副模型 Gemini，输出上限不是约束 → 若有截断，是插件默认 4000 造成的。

已做：
0. 数据分辨完成，见 18.7。原第 0 项内容保留如下供复用：
   **用数据分辨假设。** 面板「更多 → 导出 JSON」，把文件放到工作目录。看三样：
   - 覆盖亲属出场楼层的那些窗口，`entries` 条数与 summary 总长（逼近 4000 token 即截断嫌疑）；
   - 这些窗口的 events 里 `characters` / `summary` 有没有亲属的名字（有名字但 `people` 没有 → 提示词问题；连 events 都没有 → 材料抠取或模型忽略）；
   - `people` 现有条目的 `first_idx` 分布（是否全部集中在每个窗口的前几楼）。
   分辨结果决定 A–D 哪几条是必做、哪几条可缓。

待拍板：
2b. H 任意两人关系行、I 按人阶段一句、D 截断先压字数再拆半、J 块首重写——四项是否采纳。
3. `tier` 三档命名（主/配/龙套）与「龙套不进注入块」。
4. 没有正式名的人用「{{name1}}的母亲」作规范名（面板与注入块都这样显示，出现真名后自动升级）。
5. 本节先于 §17 向量落地。
6. 可选：面板段落卡里顺手看一眼「ok」窗口的条目数与人物数，能进一步坐实截断这条。

### 18.7 数据核对结果（0903，用户导出 114 楼聊天的记忆 JSON；主角 {{user}}=陆昭宁、{{char}}=沈行知，副模型 gemini-3.1-pro-preview）

| 项 | 数据 |
|---|---|
| 窗口 | 4 个，全部 ok，各 16/16/15/11 个 AI 楼 |
| 每窗事件数 / summary 总字数 | 4/492、4/326、2/164、5/508 |
| 人物志 | 3 人（张姐 #8、郑晚晴 #100、陆远洲 #104），每人 8 字段全部来自同一楼，first_idx == last_idx |
| 事件 characters 里出现的非主角 | 8 人：张姐、郑晚晴、赵明德、陆远洲、小刘、方敏之、韩知予，另 tags 里有郑泽远 |
| 缺席亲属（用户核对：原文 9 楼写了名字） | 陆昭宁的母亲。summary 里出现 4 次，全部写成「母亲 / 陆母」，从未写名字；不在任何 characters，不在 people |
| 孤立条目 | 3 条，挂在一个已不存在的窗口 `w_mtl5ss4r4ogx` 下，内容与窗口 1 的前三条近似重复，status=orphan（不注入，但占面板与导出） |

**判定：**

1. **截断假设不成立。** 每窗输出只有 2–5 条事件、164–508 字摘要，人物卡也是完整的，离 4000 token 差一个数量级。18.1 第 1 条撤销；`finish_reason` 检查降为卫生项。
2. **「提示词让模型少写人」成立。** 事件里点到 8 个非主角，档案只建了 3 个。最硬的一例：窗口 2（#32–#62）是妻子郑晚晴（#32）和赵明德（#36）首次登场，该窗 people 为空，郑晚晴直到窗口 4 的 #100 才建档。三个人的字段全部来自单一楼层且再没更新过，说明「只填有变化的」被执行成「只在第一次写、之后不再看」。
3. **「不在场亲属被折成称谓」成立。** 母亲在 #18–#26 以微信出现、在 #108–#112 「经母亲提醒」直接触发了 S 级事件「发现已婚」，是推动主线的人，但摘要一律写「母亲」，名字被丢掉；因为她不在场，进不了「在场且有行动的人」，也没被当成需要建档的「配角」。
4. **额外发现：事件本身也是梗概式的。** 16 个 AI 楼压成 4 条；酒店一场 15 楼压成 2 条共 164 字。这就是参考版批评的「剧情梗概而非回忆卡片」，与人物漏网同源：模板把副模型定位成「档案员」，它就按档案的密度写。
5. **不可核实项：** 模型是否曾以非数组形状返回 people（代码会静默忽略）。需要保留最近若干次原始回复才能判断。
6. **Bug：** 孤立条目来源不明。删窗口的两条路径（拆半、面板删段）都连带删条目，说明另有路径让窗口消失而条目留下；待用户回忆操作序列后复现。

**对 18.3 改法的取舍（数据驱动）：**

- 必做：A 点名表置前（治 2、3）、B 建档与更新分离 + 亲属独立建档明文（治 2、3）、C 事件反哺（立刻补 4–5 人，零网络）、F 保留原词（治 3 的「名字被折成称谓」、治 4）、H 任意两人看法（母亲对沈行知的态度正是主线信息）。
- 新增：**K 事件密度指标**——每窗事件数下限改为「每 4 个 AI 楼至少 1 条」并写进模板（现在只有上限 `max_events`）；summary 上限 150 字改为「80–200 字，宁具体勿概括」。
- 新增：**L 原始回复留档**——`data.debug.lastRaw[]` 保留最近 3 次副 API 原文（各截 8k 字），面板「更多」里可看，用于诊断 5。
- 新增：**M 孤立条目清理**——reconcile 时，窗口不存在且楼层已被某个 ok 窗口覆盖的条目直接删除，其余保持 orphan。
- 降级：D 截断处理为卫生项；E charIsNarrator 不做；G2 只保留 `content_filter` 当拒答。

**18.7 补充（0903，用户补充操作史）：**

用户观察：逐段自动跑（新楼攒够就总结）基本不漏人；一次性「总结到当前」58 轮历史时漏；中断一次、删除后重跑仍漏，再删再跑仍漏同一个重要 NPC；龙套 NPC 反而进了出场角色。原文 9 段回复都写了母亲的名字。

7. **批量与逐段的差别是窗口大小。** 自动模式按 `autoInterval`=20 楼攒批，每窗约 10 个 AI 楼；「总结到当前」按 `windowFloors`=40 切，每窗 16 个 AI 楼。
   导出数据显示模型的输出量不随输入增大：16 楼窗给 4 条、15 楼窗给 2 条，与 10 楼窗产出相当。窗口越大压缩越狠，人和事一起被压掉。
   这解释了「逐段不漏、批量漏」，也说明 K（密度下限）必须同时写进模板并在代码里核验（少于下限 → 视为输出不合格走拆半，与拒答同路）。
8. **龙套进、母亲出的机制。** `characters` 的定义是「在场且有行动的人」。老板娘、秘书在场有动作，天然入选；母亲通过微信、电话、提醒起作用，从不在场，天然落选。
   现有模板把「重要」等同于「在场」，与剧情影响力无关。cast 的 `tier` 必须按**对剧情的影响**判，模板明文「不在场但影响了事件走向的人算主/配，在场但只提供服务的人算龙套」。
9. **重跑两次漏同一人是确定性的。** temperature 0.3、同一模板、同一材料，模型的取舍稳定复现。重跑不是修复手段，只有改模板或改窗口才有用。
10. **名字被折成称谓的成因。** 原文对母亲的主要指称是「母亲/陆母」，名字只在 9 处出现；模型取主导指称写摘要，名字被当成次要信息丢弃。
    F（保留原词）之外，cast 的 `aliases` 正是为此：`{"name": "真名", "aliases": ["母亲", "陆母"]}`，两种指称都能命中。
11. **孤立条目的来源（代码推断，待用户确认操作）。** 「清空」把 `chatMetadata.eratoMemory` 整个换成新对象；若此时有一次副 API 调用还在飞，
    `applyWindowResult` 里的 `getData()` 拿到的是**新**对象，条目和人物被写进新库，而它们的窗口对象属于旧库、已随清空消失 → 条目 status=orphan、窗口不存在。
    与用户「中断一次、删除、重新总结」的顺序吻合。修法 **N**：数据对象带世代号，清空时世代 +1 并置 `run.stop`；在飞调用返回后世代不符则丢弃结果。

必做清单更新：A、B、C、F、H、K、M、N；tier 判据改为剧情影响（第 8 条）。

### 18.8 落地记录（v0.3.3，2026-09-03）

用户确认孤立条目来自「清空后重新总结」并下令开工。落地项与 18.7 必做清单一致：

| 项 | 实现 |
|---|---|
| A 点名表 | 模板输出顺序 `cast → events → relation → people → items`；cast 含 name/aliases/tier/role/seen；tier 按剧情影响判并明文 |
| B 建档与更新分离 | people 改「有则填、无则不写该键」；亲属独立建档明文写进「已知主角」段 |
| C 事件反哺 | `mergeEntities` 末尾把 events.characters 里的非主角建档；面板「更多 → 从事件补人物」零网络补档（`backfillPeople`） |
| F 保留原词 | summary 规则改 80–200 字 + 必须保留人名/称呼/地点/物件/动作 + 禁空话；characters 定义改为「有关的人，含不在场但影响走向的」 |
| H 任意两人看法 | `p.views[对象] = {v, trend, idx, date}`；trend 七档校验；面板可看可改；注入完整卡加「看法：」行 |
| I 按人阶段 | `arc` 进 PERSON_FIELDS，注入完整卡显示「阶段：」 |
| J 块首 | 注入块块首四行（边界声明 / 正典与往事用法 / 信息墙 / 阶段看法只是参考） |
| K 密度下限 | 设置 `minEventsPer`（默认 4，0 关）；模板给 `{{min_events}}–{{max_events}}` 区间；`applyWindowResult` 少于下限抛 split 走拆半 |
| L 原始回复留档 | `data.rawLog` 保留最近 3 次（各 6000 字），面板新分区「副 API 最近的原始回复」 |
| M 残影清理 | `reconcile` 删除「窗口不存在且楼层已被正常窗口覆盖」的条目，其余保持 orphan |
| N 清空世代 | `runWindow`/`ingest` 以数据对象身份判断库是否被换，换了就作废本次结果；清空按钮置 `run.stop` |
| D 卫生项 | `callApi` 返回 `{text, finish}`；`length` → 带长度约束重来一次再拆半；`content_filter` → 按拒答走拆半 |
| 别称升级真名 | 新名字带着旧档案的名字/别称来 → 改名并把旧名放进 aliases（「陆母」→「王秀英」） |

桩测 30 项全过（新增：密度换算、事件太少拆半、截断重试、清空世代、残影清理、补人物、点名/改名/views/arc 合并、龙套不注入）。
用真实导出干跑：残影条目 18→15，补人物 +4（赵明德、小刘、方敏之、韩知予）。

未做 / 待实测：`charIsNarrator`（E，本次不是原因）；tier 对注入预算的排序（先只做龙套不注入）；密度下限对真实副模型的拆半次数；母亲能否以真名进点名表（实测对象）。

## 19. 人物志 / 物件分区化与分类（v0.3.4，2026-09-04）

用户提出：出场人物与物品分开显示、像时间线一样可筛；人物分主/配/龙套；物件按关键性分，且要有「完成使命」之类的实时状态。讨论定案后一次落地。

### 19.1 顺手查出的老问题

`mergeEntities` 每个窗口无条件覆写 `tier`：主角色在某段只被提一句、副 AI 判「配」或「龙套」，就被降档；面板手改的档位也会被下一窗口冲掉。分类无论怎么定，这条必须一起修 → 档位只升不降 + 手改锁。

### 19.2 定案

| 项 | 定案 |
|---|---|
| 面板 | 「人物与物件」拆成「人物志」「物件」两个分区，各带筛选条与排序；关系现状一行留在人物志分区顶部 |
| 人物档位 | 主/配/龙套三档不加（四档以上副 AI 判得更飘；档位只回答「档案值不值得注入」）。**只升不降**；面板手改后 `tierLock`，选回「未定」解锁 |
| 自动升配 | 龙套/未定进过 S/A 事件的 `characters` → 配；累计 `seen ≥ 3` 段 → 配。单窗口视角判不准，聚合数据比模型准（§18 漏人问题的延续） |
| 人物状态（第二条轴） | `state` 枚举：在场（默认）/ 离场（剧情明确离开）/ 死亡 / 下落不明。最新一窗为准，手改 `stateLock`。原自由文本 `status`（现状一句）保留。档位=剧情权重（慢变），状态=生命周期（事件驱动），两轴正交 |
| 活跃度 | 不用副 AI，面板从 `last_idx` 算：近期 = 最近 `npcScanDepth` 楼提到（与注入完整卡同一判断，面板看得出谁在吃预算）；沉寂 = 超过 `DORMANT_FLOORS`(100) 楼未露面；其余在册 |
| 物件关键性 | `tier`：关键（推动剧情或承载关系，没了故事会变）/ 次要（有意义但可替代）/ 摆设（只是道具，不注入，对应龙套）。只升不降 + 手改锁 |
| 物件状态 | `state`：待用 → 在用 → **已使用**（一次性物件完成使命）；已转手 / 遗失 / 损毁 / 封存。最新为准 + 手改锁。原自由文本 `status` 改名 `note`（备注），读取时迁移 |
| 注入 | 摆设不注入；已使用/遗失/损毁/封存（`ITEM_SETTLED`）降成一行「已了结：名（状态·持有者）」——「信已经烧了」要记住但不值一张卡；已转手不算终态（东西还在戏里）；人物卡「状态：」只在非在场时占字 |
| 提示词 | people 加 `state`，items 加 `tier`/`state`、`status`→`note`；档案规则补三条（人物生命周期、物件关键性、物件状态含「变了就要更新」）；点名表段的物件清单带 `[档位](状态)` 让副 AI 知道现状 |
| 筛选 | 人物：档位 / 状态 / 活跃度 / 搜索（名字、别称、身份）/ 排序（最近露面、首见、名字、露面段数）。物件：关键性 / 状态（含「已了结」合并项）/ 持有者（下拉项由现有值生成）/ 搜索（名字、持有者、备注、意义）/ 排序 |
| 数据 | 人物加顶层 `state`/`tierLock`/`stateLock`，物件加 `tier`/`state`/`tierLock`/`stateLock`；旧数据键为空显示「未定」，零迁移（除 `f.status`→`f.note`）；自定义模板仍按老键 `status` 回的物件也落到 `note` |

不做（讨论中明确排除）：「群体/组织」实体（家族、公司、帮派会被丢掉或硬塞成人，是真缺口但属另一批）；四档以上人物分级；由 holder 变化自动推「已转手」（让副 AI 判比猜稳）。

### 19.3 落地记录

桩测 36 项全过（新增 6：档位只升不降与手改锁、状态与物件关键性、终态物件降一行、自动升配（≥3 段 / A 事件升、B 事件不升）、活跃度与四类筛选、旧数据 status→note）。既有用例调整：假副 API 后段故意把陆衍报成龙套验只升不降；司机老王只露一段验不误升；小刘因进过 S 事件应为配。

待真机实测：副模型对 `state`/`tier` 枚举的遵守率（乱写会被忽略、显示「未定」）；自动升配阈值 3 段是否把常驻龙套（司机、管家）升得太多；「已了结」一行是否够主模型认出「那封信已经烧了」；持有者下拉在物件多时是否可用。

## 20. v0.4.0 长期不失忆批次：分层压缩 + 召回 + 存储硬化（2026-09-04）

起点：对照一款分层记忆扩展（逐楼摘要 → 每 15 回合锚点 → 每 3 锚点累计合并 + 状态索引 + 混合召回）逐部件审计，
把「失忆」拆成四种分别治：①没记下（摘要取舍）②记了没给（预算裁剪）③给了走样（逐级压缩失真）④记了被冲掉（重跑/删除/LLM 复活）。
用户定案：必做 + 应做 + §17 向量一批做完；两条折叠规则原文进远景层提示词。

### 20.1 代码里确认的三个「被冲掉」洞（v0.3.4）

1. `applyWindowResult` 重跑窗口时 `entries.filter(e => e.win !== w.id)` 把钉选与手改过的条目一并扔掉。
2. 面板删掉的人物/物件没有墓碑，下一窗口 `upsertPerson` 原样复活。
3. `setF` 无条件覆盖，手改的档案值被下一窗冲掉（v0.3.4 只锁了 tier/state）。

### 20.2 参考对象的取舍（只借机制不搬文字）

| 部件 | 它 | 我们 | 取舍 |
|---|---|---|---|
| 逐楼摘要层 | 每楼一次副 API | Erato 每楼自带 `<recap>` | **借层不借调用**：recap 存成逐楼底层，零成本 |
| 三层压缩 | 固定间隔累计重写（有损递归） | 无（预算裁掉即消失） | 等级驱动的非破坏压缩：正典压缩 + 远景层，条目永远在库 |
| 折叠规则 | 「同日同目标合并一条」「压措辞不删事实」 | — | **原文借用**，进远景与正典提示词 |
| 楼级覆盖 | 按楼摘要天然完整 | 按事件切，可整楼跳过 | 覆盖核验 + 纠正式重试 |
| 截断判定 | 结束哨兵 + 内容完整性优先于 finish_reason | 只看 finish_reason + 补闭合 | 哨兵 `<END_OF_MEMORY/>` |
| 保底 | 摘要失败楼层注入裁过的原文 | 无 | 用 recap 保底（主模型自己写的摘要，质量更高） |
| 召回 | n-gram 关键词 + embedding 混合，IndexedDB | §17 草案 | 关键词通道零网络先行；向量走酒馆服务端；加 `<content>` chunk 细节召回 |
| 出站原地替换旧楼 | prompt-ready 改出站数组 | is_system 隐藏 + depth 6 单块 | 不借（与预设正则协同，用户主动要隐藏） |
| 每楼注入回看 | 逐楼存引用 | 只留最后一次预览 | 不做；召回落地后只留最近 N 轮召回记录 |

### 20.3 数据结构增量（DATA_VERSION 仍为 2，字段缺省补齐）

```js
data.recaps    = { [send_date]: { i: idx, t: '日期 · 时段 · 场景', n: '叙述 ≤400 字' } }   // 逐楼底层，reconcile 时补新楼、删失效楼
data.outline   = { lines: [ { id, key, text, from: [entryId], idx, last, level: 1|2, at } ], pending: [entryId], at }
data.canon     = { text, builtFrom: [entryId], builtAt }        // 原有空壳，本批启用
data.tombstones = { p: { [norm名]: { name, at } }, i: {...} }
data.vec       = { entries: { [entryId]: numHash }, raw: { [send_date]: { h: contentHash, hs: [numHash] } }, source, model, at }
Entry.locked   = true    // 用户改过标题/摘要/等级 → 重跑保留、副模型被告知勿重复
Window.uncovered = [idx] // 副模型申报无事 + 核验补出的未覆盖楼
人物/物件字段值.manual = true → 副模型不覆盖；.stale = true → 来源楼已不存在（待核）
```

### 20.4 摘要阶段（治①与④）

- **结束哨兵**：模板末尾要求 JSON 之后另起一行 `<END_OF_MEMORY/>`。缺哨兵 = 截断（无论 finish_reason），走压缩重试；仍缺再拆半。自定义模板不含哨兵字样时不核验。
- **覆盖核验**：新输出键 `uncovered: [{floors, why}]`（放末尾，截断先切它无碍）。代码算「窗口楼 − 事件认领 − 申报」= 漏楼；漏楼 > 窗口楼数 1/3（窗口 ≥3 楼）→ 纠正式重试：把漏楼号附回去要求补写或申报。
- **纠正式重试先于拆半**：JSON 不合法、事件太少、覆盖不足三种情况各允许一次纠正（附上次输出 + 问题点名），仍不合格才拆半；拒答直接拆半。理由：18.7 第 9 条，同温度重跑复现同一遗漏，只有点名才会补。
- **喂骨架**：模板新增 `{{context}}` = 远景最近 8 行 + 正典（压缩文或最近 12 条 S 标题），≤1500 字，供承接因果与去重。
- **锁定条目回喂**：`{{locked}}` = 本窗口钉选/手改条目，要求「保留原样，不要重复记录，其余照写」。

### 20.5 存储硬化（治④）

- 重跑/拆半保留 `pinned || locked` 条目（拆半时按楼归到子窗口）。用户改标题/摘要/等级即 `locked`，卡片显示 🔒，可解锁。
- 墓碑：删人物/物件时把名字与别称记进 `tombstones`，副模型再报同名直接忽略；面板人物志底部列出「已删除」可点解除。「从事件补人物」同样尊重墓碑。
- 字段锁：手改过的字段值 `manual`，`setF` 跳过；views 按对象锁；关系现状手改后 `manual` 锁。卡片展开有「解除字段锁定」。
- 待核：reconcile 时字段值的来源 `date` 在聊天里找不到 → `stale`，面板显示 `?`；不改注入。

### 20.6 三层压缩（治②，限制③）

| 层 | 内容 | 预算 | 何时生成 |
|---|---|---|---|
| 远景 | 被预算裁掉的条目按故事日折成一行（60–100 字）；远景总量超 `outlineChars` 时最老的若干日行再折成一条「时期」行（level 2） | `outlineChars` 2000 | 治理任务：裁掉且未折叠的条目攒够 `foldMin`（6）条，或总结批次结束时 |
| 正典 | S/钉选。超过 `canonMax`（25）条时压成 ≤`canonChars`（800）字的正典段；压缩后新增的 S 逐条附在段后；S 集合相对上次压缩变化 ≥5 条重压 | 不受往事预算约束 | 治理任务 |
| 往事 | A/B/C 完整条目 + recap 保底行 | `maxInjectChars` | 每轮 |

- **非破坏**：折叠只写 `outline.lines`，条目本体不动；被远景覆盖的条目不再进往事（避免重复），仍参与召回、面板可见。
- **折叠材料** = 被裁条目 + 这些条目所在楼的 recap（每日 ≤3 条、各 ≤200 字）。
- **折叠提示词硬规则（原文）**：
  - 同一剧情日、同一目标连续推进的多个场景合并成一条；只有日期、核心矛盾、目标或关系阶段发生变化才另起一条。
  - 压措辞不删事实：人名、日期、关键物件、承诺、决定、伤害/失去/获得必须保留。
  - 已有骨架与新增材料合并重写而非追加；不加材料里没有的事实；第三人称、不评价不预测。
- 正典压缩用同一套规则，输出纯文本行。
- 治理任务与总结共用副 API，不与窗口并发；不阻塞生成。

### 20.7 注入块（治②）

顺序：块首 → 关系现状/人物志/物件 → `## 远景` → `## 正典` → `## 往事`（条目与 recap 保底行按楼号交错）→ `## 相关往事`（召回）→ `## 原文细节`（chunk 召回）。

- **recap 保底**：可见深度 ≥ `recapWindow`（20，与预设 5🌸 minDepth 一致）或已隐藏、且未被 ok 窗口覆盖（未总结 / 失败 / 拒答 / 窗口申报未覆盖）的楼，注入 `- {t}「#idx·作者摘要」{n}`；没有 recap 的合并成区间一行「#a–#b 楼尚未整理，正文不在上下文，不要补写这段细节」。深度 <20 的楼预设自己还带着 recap，不重复。
- 人物激活词加身份字段（「秘书」「母亲」也能命中）；关键物件附「履历」行（从条目里按名字匹配抠楼号+标题，零 LLM）。
- 块首加一行：远景 = 更早时期的骨架。

### 20.8 召回（§17 落地 + 增补）

- **关键词通道**（默认开，零网络）：查询串 = 最新用户输入（去 OOC 括号）+ 最近一条 AI 楼 recap 叙述，≤`queryChars`；中文 2–4 gram + 拉丁词，IDF 加权，候选 = 被裁/已折叠的 ok 条目。
- **向量通道**（默认关）：源 `siliconflow`（`{model, siliconflow_endpoint}`，密钥槽 `api_key_siliconflow`）/ `vllm`（`{apiUrl, model}`，`api_key_vllm`）/ `openai`。集合 `erato-mem-<hash(chatId)>`（条目）与 `erato-raw-<hash(chatId)>`（已总结楼层的 `<content>` 按约 600 字带重叠切块，只对已入库楼层）。`/api/vector/query` 只回过阈值的 metadata 不带分数，按返回顺序赋名次分。
- **预取**：`MESSAGE_SENT` 时开始查询；拦截器最多等 `timeoutSec`（4 秒），超时降级关键词，绝不阻断生成；按查询串缓存，swipe 不重复调用。
- 融合：两通道都命中加权；按分填 `recallChars`，输出按楼号升序。原文细节独立预算 `rawChars`。
- 维护：窗口 ok 后插入该窗条目 + 该窗楼层 chunk；条目改/删同步；切聊天对账（`list` 差集）；「重建向量」purge + 全量；「补向量」只补缺的；「写入密钥到酒馆」走 `/api/secrets/write`。
- 密钥槽会覆盖酒馆同名槽位，设置页明示。

### 20.9 稳定性

- 手动「总结到当前」在生成中拒绝执行；生成结束 2 秒内等待稳定再开跑。
- `run.stop` 同时 abort 在飞的副 API 请求；被中止的窗口保持 pending、不计失败次数。

### 20.10 设置项增量

| 键 | 默认 | 说明 |
|---|---|---|
| recapWindow | 20 | 与预设 5🌸 minDepth 一致；深度 ≥ 此值或已隐藏的未整理楼才注入 recap 保底 |
| canonMax / canonChars | 25 / 800 | S 超过条数即压缩；压缩段字数上限 |
| outlineChars / foldMin | 2000 / 6 | 远景预算；攒够几条被裁条目才折叠 |
| recall.keyword | true | 关键词召回 |
| recall.vector / source / apiUrl / model / cn / raw | false / siliconflow / '' / 按源默认 / true / true | 向量召回；raw = 原文细节 |
| recall.topK / threshold / recallChars / rawChars / queryChars / timeoutSec | 8 / 0.4 / 2000 / 1200 / 600 / 4 | |

### 20.11 推翻的旧定案

- §17.1「归档合并与按日大纲作废」撤销：召回是点状的，主模型仍需要连续骨架；远景层即按日大纲的落地形态。§8.2 归档合并仍作废（远景替代）。
- §15.5「手改档案值会被覆盖，需要时加锁」→ 本批加锁。
- §14.7「先关键词优先替代向量」→ 关键词通道与向量通道同批落地，关键词默认开、向量默认关。

### 20.12 实测清单（v0.4.0）

1. 哨兵：正常输出末尾有标记；人为把 max_tokens 调小 → 面板原始回复显示压缩重试。
2. 覆盖：某窗口副模型跳楼 → 原始回复里看到「纠正重试：覆盖」；`uncovered` 楼在注入块里以 recap 保底出现。
3. 锁：改一条摘要 → 🔒；编辑该窗口某楼触发重跑 → 该条仍在。删一个人物 → 下次总结不复活；解除后可复活。
4. 远景：把 `maxInjectChars` 调小逼出裁剪 → 攒够 6 条后治理折叠 → 注入块出现 `## 远景`，被折叠条目不再出现在往事；面板远景分区可改可删。
5. 正典：把 `canonMax` 调到 3 → 正典段生成，之后新 S 逐条附后。
6. recap 保底：故意让一段拒答 → 注入块里该段楼层以作者摘要出现；无 recap 的楼出现区间占位行。
7. 召回：`maxInjectChars` 调小后，输入提到早期人物/物件 → `## 相关往事` 出现对应条目；无关话题为空。向量：测试连接、重建、补向量、密钥写入各一次；弱网 4 秒放行。
8. 停止：总结中点停止立即中断在飞请求，段落保持待处理。

### 20.13 落地记录（v0.4.0，2026-09-04）

全部按 §20.3–§20.10 落地，桩测 55 项全过（新增 19：recap 底层、哨兵判定与缺哨兵重试、覆盖不足纠正重试、申报未覆盖走 recap 保底、失败楼层保底与区间占位、待核标记、锁定条目重跑保留并喂回、拆半归子窗口、墓碑与恢复、字段/看法/关系锁、身份词激活与物件履历、远景折叠、时期折叠、正典压缩与骨架回喂、关键词召回、生成前召回与相关往事、向量对账/查询/原文细节/重建/密钥写入、原文切块、密度纠正重试、停止中止、旧数据补齐新字段）。

实现细节与设计稿的出入：
- `Window.uncovered` 存 send_date 而非楼号（删楼后仍能对上）。
- 拒答回复通常也没有哨兵：先判拒答再判截断，避免对拒答做压缩重试。
- 关键词通道把「数字+汉字」组合（7号、3楼）单独当词，纯汉字 n-gram 会把数字切掉。
- 远景折叠一次调用折完全部待折叠分组；某分组没返回文本时留在队列下次再折；治理连续失败 3 次暂停到下次总结批次或手动「现在折叠」。
- 治理任务与总结批次不并发（`run.busy` 时跳过），但与主模型生成可以并行。
- 向量库的 `/insert` 回 `OK` 文本而非 JSON，`vecCall` 对非 JSON 回包直接返回文本。

未做：§17.9 第 4 项（召回激活人物志完整卡）留二期；向量阈值 0.4 与 topK 8 未经真机校准。

### 20.14 删楼回滚与可编辑面（v0.4.0 同批补齐，2026-09-04 晚）

起点：用户问「20 楼自动总结后删掉几楼，记忆能回滚吗」。逐层核对的结论：事件层能（窗口缩楼 → stale → 重跑），档案/远景/正典不能，锁定条目有小洞；注入块本身是每轮重算的视图，没有回滚问题，删楼到重跑之间由 recap 保底顶住。补法：

| 层 | 机制 |
|---|---|
| 档案字段 / 看法 / 关系现状 / 状态 | 覆盖前旧值压进 `hist`（3 版）；`rollbackValues(data, isDead)` 把来源楼已死的值退回最近一个来源楼还活着的旧版，退无可退清掉；手改值不退只标待核。首见楼与全部值都死 → `lost`（不注入不删，再被提到或手动保存即恢复） |
| 远景 | 日行记 `dates`；任一楼没了或所在段落非 ok → 行作废，其条目退回待折叠 |
| 正典 | `builtFrom` 里有 S 消失 → 立即重压（只新增仍攒 5 条）；S 降回阈值内 → 清掉压缩段逐条注入 |
| 锁定条目 | 来源楼全不在窗口里的不再保留（重跑与拆半都是） |
| 撤销本段 | 条目卡「撤销本段」：删窗口与条目 + 同一套 `rollbackValues` 按窗口楼层回退 + 远景作废 + 向量删除 + 楼层取消隐藏；楼层不动，之后可重新总结 |

`isDead` 两种：删楼对账 = send_date 不在聊天里；撤销本段 = send_date 落在该窗口。

可编辑面（同批）：正典压缩段可点击编辑，保存即锁定（删 S / 加 S 都不自动重压，解锁或「重建正典」恢复）；远景手改行折叠时不再覆盖，同日新条目另起「key·续」行，时期折叠也跳过手改行；注入块块首说明进设置「提示词」组（留空用默认）；手动模式下段落因删楼/改楼过期时提醒一次。

其余：自动整理三档 `autoFold`（batch 默认 = 总结批次后 / always = 每次生成后攒够就跑 / manual），自动跑完弹一次「记忆整理：…」；注入预览顶部分段计数（档案/远景/正典/往事/保底/召回/原文）；故事日键归一化（全角数字、汉字数字、「号」→「日」，**年份保留**，长记忆会跨年）；向量通道失败不再连带丢掉关键词命中；导入 JSON 的替换/合并两种模式补上远景/正典/recap/墓碑，向量索引导入后置空。

桩测 59 项（新增 4：删楼回滚全套、故事日归一化、远景按楼作废+块首自定义、远景手改续行、正典丢 S 重压/锁定/降阈值清段、撤销本段）。

### 20.15 向量配置改成与副 API 同法（v0.4.1，2026-09-05）

用户装上 v0.4.0 后反馈：向量设置填不了地址、拉不了模型，只能手填模型名；且向量属高楼层可选项，应放在副 API 正下方。原因是 §20.8 的「源」下拉把地址栏限定在 vllm 源下，默认硅基流动时地址被隐藏，而模型只能手填。

改法：去掉源下拉，**统一走酒馆的通用 embeddings 路线（`source: 'vllm'`）**——任何 OpenAI 兼容 embeddings 地址都能用，硅基流动填 `https://api.siliconflow.cn/v1` 即可（后端 `trimV1` 后自己拼 `/v1/embeddings`）。设置三件套与副 API 完全一致：地址、密钥、「拉取向量模型」+ 下拉选择 + 手填框；拉模型复用副 API 那条 `/api/backends/chat-completions/status` 路（后端拿 reverse_proxy + proxy_password 打 `{base}/models`），像 embedding 的模型（embed/bge/e5/gte/minilm/jina/nomic）排前面。密钥存在设置里，`ensureVecSecret()` 在每次向量调用前按密钥哈希判断，只写一次 `api_key_vllm` 槽位，换密钥自动重写；「写入密钥」按钮删除。整组挪到副 API 下方，标题「向量召回（高楼层可选）」，关键词开关与预算参数收在组尾。旧设置里的 `siliconflow`/`openai` 源加载时换成对应官方地址（`LEGACY_VEC_URL`）。桩测 59 项（向量用例改写：拉模型排序、/v1 处理、密钥只写一次与换密钥重写、旧源迁移）。
